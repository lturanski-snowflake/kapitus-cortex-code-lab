### 2.4 — Insert New Applications to Score

```
Run the file /sql/04b_insert_new_applications.sql EXACTLY as written to insert 
50 new PENDING loan applications into SOURCE_DATA.LOAN_APPLICATIONS. Do NOT 
improvise — use the file verbatim. It inserts 35 normal + 15 suspicious apps.

Then refresh my LOAN_ANALYTICS_MART:
ALTER DYNAMIC TABLE LOAN_ANALYTICS_MART REFRESH;

Verify: SELECT COUNT(*) FROM LOAN_ANALYTICS_MART WHERE APPLICATION_ID LIKE 'NEW-%';
```

