### 0.1 — Environment Setup (if prework was not completed)

```
Please run the environment setup SQL from /sql/01_environment_setup.sql to create the 
KAPITUS_TRAINING database, warehouse, schemas, role, and all required grants. 
Run it as ACCOUNTADMIN.

STOP — before running Step 3 (Per-Attendee Schemas): The schema names in the file 
(TRAINING_LUKE, TRAINING_ALEX, etc.) are EXAMPLES ONLY. Do not create them. 
Instead, ask me now: "Which attendee schemas should I create?" 
Wait for my response before proceeding.

STOP — before running Step 7 (Assign Role to Attendees): Read the file 
attendees.txt in this workspace. It contains a list of Snowflake usernames 
(one per line). Grant KAPITUS_TRAINING_ROLE to each user in that file. 
If attendees.txt does not exist, ask me: "Which Snowflake users should I 
grant KAPITUS_TRAINING_ROLE to?" and wait for my response.

After the setup SQL completes, run /sql/02_sample_data.sql EXACTLY as written — do 
NOT improvise or create different tables. The file creates exactly these 4 tables 
in SOURCE_DATA:
  - BORROWERS (5,000 rows)
  - LOAN_APPLICATIONS (20,000 rows)  
  - LOAN_DECISIONS (15,000 rows)
  - PAYMENT_HISTORY (50,000 rows)

Do NOT create any other tables (no LOANS, PAYMENTS, UNDERWRITING_NOTES, or 
SUPPORT_TICKETS). Read the file and execute it verbatim.

Finally, switch to KAPITUS_TRAINING_ROLE and set context to KAPITUS_TRAINING 
database and KAPITUS_TRAINING_WH warehouse.
```

---

**Before running this prompt**, create an `attendees.txt` file in your workspace with Snowflake usernames (one per line):

```
JSMITH
MJONES
KPATEL
```

This file is used by the setup to grant `KAPITUS_TRAINING_ROLE` and create `TRAINING_<USERNAME>` schemas for each attendee.
